// Novel title max/min length
export const NOVEL_TITLE_MAX_LENGTH = 100;
export const NOVEL_TITLE_MIN_LENGTH = 0;

export const TITLE_LENGTH = {
  max: NOVEL_TITLE_MAX_LENGTH,
  min: NOVEL_TITLE_MIN_LENGTH,
};

// Novel content max/min length
export const NOVEL_CONTENT_MAX_LENGTH = 140;
export const NOVEL_CONTENT_MIN_LENGTH = 0;

export const CONTENT_LENGTH = {
  max: NOVEL_CONTENT_MAX_LENGTH,
  min: NOVEL_CONTENT_MIN_LENGTH,
};

// Novel summary max/min length
export const NOVEL_SUMMARY_MAX_LENGTH = 280;
export const NOVEL_SUMMARY_MIN_LENGTH = 0;
export const SUMMARY_LENGTH = {
  max: NOVEL_SUMMARY_MAX_LENGTH,
  min: NOVEL_SUMMARY_MIN_LENGTH,
};

// Default max length for inputs
export const DEFAULT_MAX_LENGTH = 999;
export const DEFAULT_MIN_LENGTH = 0;

export const SEC_IN_MS = 1000;
export const DAY_IN_SEC = 60 * 60 * 24;
export const HOUR_IN_SEC = 60 * 60;
export const MIN_IN_SEC = 60;
export const MIN_IN_MS = SEC_IN_MS * MIN_IN_SEC;
export const HOUR_IN_MS = SEC_IN_MS * HOUR_IN_SEC;
export const DAY_IN_MS = SEC_IN_MS * DAY_IN_SEC;
